# ── Required ─────────────────────────────────────────────
OPENAI_API_KEY=sk-blabla-put-your-api-key-here

# ── Optional (defaults shown) ────────────────────────────
# MODEL_NAME=gpt-4o-mini
# MENTOR_DB=mentor.db
# THREAD_ID=default-thread
# CHROMA_DIR=chroma_db
# MEMORY_TOP_K=10
# DEDUP_THRESHOLD=0.92
# MAX_HISTORY_MESSAGES=20

# ── Backfill configuration ───────────────────────────────
# Enable automatic backfill on startup (default: false)
# Backfill syncs SQLite memories to Chroma vector store.
# Set to true only if you want automatic sync on every startup.
# Otherwise, use --backfill flag for explicit maintenance.
# ENABLE_BACKFILL_ON_STARTUP=false

# Number of memories to process per batch during backfill (default: 100)
# Larger batches are faster but may hit API rate limits.
# BACKFILL_BATCH_SIZE=100

# Delay in seconds between batches to avoid API rate limits (default: 1.0)
# Set to 0 to disable rate limiting.
# BACKFILL_RATE_LIMIT_DELAY=1.0
